#系统级别参数，不会经常变动
spring:
  application:
    name: cheetah-web-backstage
  cloud:
    config:
      uri: http://cheetah.zylsite.top/config
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://cheetah.zylsite.top/eureka/
server:
  port: 9095